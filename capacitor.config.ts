import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krishahlawat.dukaankonnect',
  appName: 'dukaankonnect',
  webDir: 'dist'
};

export default config;
